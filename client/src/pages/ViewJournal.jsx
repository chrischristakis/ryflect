import { useParams } from 'react-router-dom';

function ViewJournal() {

    const { id } = useParams();

    return (
        <div>
            <h3>hi</h3>
        </div>
    );
}

export default ViewJournal;
